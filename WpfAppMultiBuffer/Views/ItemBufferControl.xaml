<UserControl x:Class="WpfAppMultiBuffer.Views.ItemBufferControl"
             x:Name="ItemBuffer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfAppMultiBuffer.Views"
             mc:Ignorable="d" MinHeight="150" MinWidth="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" MinHeight="25"/>
            <RowDefinition Height="10*" MinHeight="100"/>
        </Grid.RowDefinitions>
        <TextBlock Margin="10,10,10,0" 
                   TextWrapping="Wrap" 
                   Name="HeadItem" 
                   TextAlignment="Center" MinWidth="50" MinHeight="10"/>
        <TextBox Name="BodyItem" 
                 Margin="10" 
                 Grid.Row="1" 
                 TextWrapping="Wrap" 
                 Text="" 
                 HorizontalContentAlignment="Stretch" 
                 VerticalContentAlignment="Stretch" 
                 HorizontalScrollBarVisibility="Visible" 
                 VerticalScrollBarVisibility="Auto" 
                 IsReadOnly="True" MinWidth="50" MinHeight="25"/>

    </Grid>
</UserControl>
