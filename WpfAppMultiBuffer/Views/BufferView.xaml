<Window x:Class="WpfAppMultiBuffer.Views.BufferView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppMultiBuffer.Views"
        mc:Ignorable="d"
        Title="BufferView" Closed="Window_Closed" MinWidth="250" MinHeight="250" 
        WindowStartupLocation="CenterScreen" 
        HorizontalAlignment="Left" 
        VerticalAlignment="Top"
        Height="{Binding Height, ElementName=MainPanel, UpdateSourceTrigger=PropertyChanged}"
        Width="{Binding Width, ElementName=MainPanel, UpdateSourceTrigger=PropertyChanged}">
    <WrapPanel x:Name="MainPanel" HorizontalAlignment="Left" MinWidth="200" MinHeight="200" VerticalAlignment="Top" >
        <WrapPanel.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF2D3EAC" Offset="0"/>
                <GradientStop Color="#FF30B093" Offset="1"/>
            </LinearGradientBrush>
        </WrapPanel.Background>
    </WrapPanel>
</Window>
